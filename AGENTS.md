# WoW 335 — AI operating contract
Target only github12wykrzyk/335, WoW 3.3.5a build 12340, Windows x86. The selected exact Wow.exe is pinned in runtime/client_exe_target.json (SHA256 and size); client-audit and updater must reject different bytes even if their build label matches.
Read in order AGENTS.md, AI_START_HERE.md, AI_INDEX.json, CURRENT.json, runtime/current.json, then affected modules and workflows. PROJECT_INSTRUCTIONS.md contains the full Polish contract.
main = accepted stable, work = development, parallel only if it exists, feature/* isolated experiments, promote/* verified promotion. Never copy WoW 1.12 offsets/binaries.
Current manifest and exact source lineage determine active runtime. Empty manifest means NO GAME PACKAGE. Fail closed; never claim x86 smoke = game DLL test.
One logical multi-file commit; no force push. Routine: python tools/verify_repo.py; python tools/verify_current.py; python -m unittest discover -s tests -v.
TEST: compile actual affected Windows x86 modules, package only verified exact runtime, run tools/verify_candidate_package.py --finalize; publish only FINAL_PACKAGE: PASS. User tests exact SHA. STABLE: curate accepted exact bytes on current main via promote/*, validate exact promotion SHA, then promote selectively.
No automatic cross-branch promotion. Check HEAD/Actions after interrupted streams. AI handles files, code, GitHub, builds, rollback. Report truthful branch/SHA/tests/packaging/required in-game test.

Iterative integration: every active DLL requires runtime/module_registry.json ownership contract (dependencies, 12340 resource hooks, chain arbitrator, x86 build inputs). tools/verify_current.py cross-checks it. A new functionality extends its mechanism owner or becomes an isolated feature experiment; no filename-only routing. CI validates declarations but never proves in-game behavior. Candidate workflow must verify current HEAD, not a prior run. Never promote an unaccepted experiment or use a synthetic x86 smoke file as a game module.
TEST build: tools/build_active.py reads every active DLL's module_registry.json MSVC x86 recipe, rebuilds changed modules and dependent DLLs, and requires the resulting SHA256 to match exact registered runtime bytes; unregistered/incomplete builds fail closed. STABLE: tools/verify_exact_runtime_artifacts.py requires SHA-addressed recoverable .dll.xz for every active DLL, and package_exact_current.py only packages those proven bytes; no fresh stable compile. A successful bootstrap workflow is NOT a runnable game package.
AI experiment routing: tools/ai_experiments.py validate/route reads runtime/ai_experiments.json. An explicit branch remains authoritative; otherwise continue one matching owner, or isolate work in feature/* after checking actual HEAD and shared resource contracts. Multiple owners are a conflict requiring review, not an arbitrary branch pick. In-game evidence and accepted package must name the identical commit SHA. Tool advice does not create a branch or replace live GitHub verification.

Updater channel contract: only work builds/publishes the universal updater; updater self-update follows canonical work HEAD regardless of selected game branch. TEST game packages follow work, STABLE game packages follow main, and feature branches require their own complete, exact-SHA verified game packages. No branch publishes a separate updater. Obsolete updater artifacts may be pruned after successful canonical publication. A retired feature-specific updater needs an exact-SHA bridge and clean rollback of feature-managed hooks before handing control to the universal updater. Never let two independent launchers load the same game runtime.
